function path = overtakePath(relSpeed, maxXPos, nFrames)
    % Should I define the overtake by a number of seconds, meters, frames?
    % Right now it just offsets the camera position by some value
    
    path = ones(nFrames, 1)*maxXPos;

end